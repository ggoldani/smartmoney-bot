"""
Rule definitions for alert engine.

Reserved for future use: custom rule definitions, condition mappings,
and configurable alert rules that can be loaded from YAML.

Currently, rule logic is embedded in engine.py (RSI, breakout, divergence).
"""
