services:
  bot:
    build:
      context: .
      dockerfile: docker/Dockerfile
    env_file:
      - .env
    volumes:
      - ./src:/app/src
      - ./docker-data:/app/data
    restart: unless-stopped
